# 贡献指南

## 工作流程

1. 阅读 `AGENTS.md` 以及与任务相关的全部设计文档。
2. 在协作书追加 `STARTED` 记录。
3. 运行官方上游预检并记录 commit。
4. 为变更新增或更新需求 ID、ADR、测试 ID 与追踪矩阵。
5. 实现最小范围改动。
6. 按 `docs/TEST-PLAN.md` 逐层验证。
7. 追加终态协作记录，明确证据和剩余风险。

## 分支与提交

- 建议分支：`feat/<scope>`、`fix/<scope>`、`docs/<scope>`。
- 提交格式：`type(scope): summary`。
- 一个提交尽量对应一个可追踪需求或修复。
- 不要把生成物、凭据、真实会话日志或供应商原始响应提交到仓库。

## 设计变更

以下改动必须先增加 ADR：

- 从阻断改为修复或中和；
- 改变默认模式；
- 扩大到无工具的模型调用；
- 保存原始参数或内容；
- 改变错误码或重试语义；
- 依赖非公共 DSH API；
- 更改流尾隔离策略。

## Pull Request 验收信息

PR/交付说明至少包含：

- 对应需求 ID 与测试 ID；
- 目标 DSH commit/tag；
- 行为变化和非目标；
- 执行的命令及退出码；
- 新 profile 的实际安装/启动证据；
- 是否验证 Web、headless、取消、资源上限、卸载恢复；
- 未完成项和风险。

## 发布前门禁

- npm/GitHub 或其他远端变更必须在执行前取得用户的单独明确确认；“继续实现”不等同于发布授权。
- 发布前必须由项目负责人确认许可证文本/标识、作者归属、目标仓库及 package 身份元数据；不得从上游链接或本机 checkout 猜测。
- `LOCALLY_TESTED`、fresh profile 或 Web/headless 证据不得表述为 `HOST_VERIFIED` 或 `PUBLISHED`。
- 发布候选必须保留 pack 内容审计、安装/卸载证据和未完成项；缺少真实 provider 或完整 Web 业务证据时，必须明确保留限制。

## 安全报告

若发现 sentinel 可被绕过并导致未验证工具调用执行，不要公开真实凭据、提示词或工具参数。提交最小合成复现、DSH 版本、插件版本、规则 ID 和影响范围。
